language: node_js
node_js:
  - "8"
branches:
  except:
  - gh-pages
cache:
  directories:
    - "node_modules"
jobs:
  include:
    - stage: test
      install: npm install
      script: npm test
    - stage: lint
      script: npm run tslint
      after_script: npm run prettier
    - stage: build
      deploy:
        script: npm run build
        provider: pages
        local-dir: build
        skip-cleanup: true
        github-token: $GH_TOKEN
        keep-history: true
        on:
          branch: master
    - if: branch = /^greenkeeper\/.*$
      before_install:
      - npm install -g greenkeeper-lockfile
      install: npm install
      before_script: greenkeeper-lockfile-update
      after_script: greenkeeper-lockfile-upload
